<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.funding.user.dao.MemberDAO">

    <insert id="insertMember" parameterType="org.funding.user.vo.MemberVO">
        INSERT INTO tbl_member (
            username, password, email, nickname, role, create_at, update_at
        ) VALUES (
                     #{username}, #{password}, #{email}, #{nickname}, #{role}, NOW(), NOW()
                 )
    </insert>

    <select id="findByUsername" parameterType="string" resultType="org.funding.user.vo.MemberVO">
        SELECT
            username,
            password,
            email,
            role,
            nickname,
            create_at,
            update_at
        FROM tbl_member
        WHERE username = #{username}
    </select>


</mapper>
